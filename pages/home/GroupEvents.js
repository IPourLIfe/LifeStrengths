import React, {Component} from 'react';
import {View, Text} from 'react-native';

export default GroupEvents = () => (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
        <Text>GroupEvents</Text>
    </View>
);